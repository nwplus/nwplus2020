language: node_js
node_js: node
notifications:
  email: false
install:
  - yarn install
jobs:
  include:
    - stage: "Lint and Test"
      name: "Lint"
      script: yarn lint
    - script: yarn test
      name: "Test"
    - stage: Build
      script: yarn build
    - stage: Deploy
      script: echo "hello world"
stages:
  - "Build"
  - "Lint and Test"
  - name: Deploy
    if: branch = master