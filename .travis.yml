language: node_js
node_js: node
notifications:
  email: false
install:
  - yarn install
jobs:
  include:
    - stage: "lint and test"
      name: "Lint"
      script: yarn lint
    - script: yarn test
      name: "Test"
    - stage: Build
      script: yarn build
    - stage: Deploy
      script: echo "hello world"
stages:
  - name: Deploy
    if: branch = master